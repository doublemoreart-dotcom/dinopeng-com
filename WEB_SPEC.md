# 網頁閱讀性與更新規範

## 目的

本規範適用於所有 `ai_industry_penetration_YYYY-MM-DD.html` 週更版本。每次更新資料時，除了更新數據與來源，也必須維持閱讀性、主題切換、字級切換與 RWD。

## 閱讀性與留白

- 頁面主內容必須置中，最大寬度建議落在 `960px` 到 `1120px`。
- 上下左右 Margin / Padding 需保留適度留白，不讓文字或圖表貼齊視窗邊緣。
- 桌面版左右留白需比手機版更寬；手機版仍需保留至少 `16px` 左右內距。
- 區塊之間使用一致間距，避免卡片、圖表與說明文字擠在一起。
- 圖表高度需固定或以 RWD 條件調整，避免載入後版面跳動。

## Light / Dark Mode

- 每個週更 HTML 都必須提供 Light / Dark Mode 切換。
- 預設可跟隨系統或使用上次選擇；若使用 localStorage，需用 `try/catch` 避免瀏覽器權限錯誤。
- Light 與 Dark 都必須維持文字、卡片、邊框與圖表背景的對比。
- 切換時需更新 `aria-pressed`，讓目前選取狀態可被輔助工具辨識。

## 字級大小

- 每個週更 HTML 都必須提供三種字級：
  - 小
  - 中
  - 大
- 預設字級為「中」。
- 字級切換需影響主要文字、卡片、標籤與數值，不只影響單一段落。
- 切換時需更新 `aria-pressed`。

## RWD

- 頁面必須符合 RWD，至少支援桌面、平板與手機寬度。
- KPI 卡片在桌面可雙欄，手機需改為單欄。
- 產業長條圖在手機需保留可讀性，產業名稱、長條與百分比不能互相擠壓。
- 控制列在窄螢幕需可換行或改為單欄。
- 圖表 canvas 必須有穩定高度，手機版可降低高度但不能過窄。

## 動態與互動回饋

- 動態應協助讀者理解資訊進場、層級與操作結果，不做持續漂浮或搶奪注意力的裝飾。
- 自動播放的主要進場時間建議為 `0.35s` 至 `0.72s`；捲動驅動的段落進場可在視窗高度的 `16%` 至 `22%` 內完成，位移距離建議為 `18px` 至 `34px`，手機版需降低位移幅度。
- 主視覺與區塊視覺可使用遮罩揭露及輕微縮放；段落標題先進場，內容模組與卡片再依各自進入視窗的時機逐段淡入，但同一內容只播放一次。
- 預設只顯示頁首主視覺；各分析區塊的主題圖若暫不使用，應以 `hidden` 保留，不占版面且不得建立 ScrollTrigger 動畫。
- 各分析主題使用一致的分隔線、標題留白與區塊間距，不以固定高度強制等高，讓資料量自然決定內容長度。
- 段落採捲動驅動時，分隔線、標題與內容需依序帶入；內容完成後保持穩定，不因反向捲動重播。
- KPI 數字動畫必須保留原始文字與 `aria-label`；GSAP 或 CDN 載入失敗時，靜態數值仍要直接顯示。
- Light / Dark Mode 切換可交叉淡化兩張主視覺，但不得造成容器高度改變或版面跳動。
- 置頂段落導覽應標示目前閱讀區塊；若加入閱讀進度，需保持低識別度，手機版應讓目前頁籤維持在可視範圍內。
- 側欄開關可使用 GSAP timeline 統一背景遮罩、側欄與內容節奏；鍵盤焦點、Escape 關閉與無動畫 fallback 必須維持可用。
- 人才能力與影響層級卡片若可點擊，需使用原生 `button`，並在共用側欄提供適用情境、能力拆解、衡量方式與實作提醒。
- 影響層級雷達圖預設只顯示五軸格線與標籤；滑鼠 hover 或鍵盤 focus 時，能力面積由中心展開並顯示節點，且不得改變卡片尺寸。
- 必須支援 `prefers-reduced-motion: reduce`；啟用時略過 GSAP 動態並停用非必要 transition。
- 使用 GSAP 時以 Core 與 ScrollTrigger 為主，並保留載入失敗降級，不得讓外部動畫套件成為閱讀頁面的必要條件。

## 圖示與 Icon

- 頁面圖示優先使用 Google Fonts Icons / Material Symbols。
- HTML 需保留 Material Symbols 載入連結：
  `<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght@400&display=swap" rel="stylesheet">`
- 功能圖示、箭頭、星等深度標記需以 Material Symbols 呈現，避免混用多套 icon 來源造成視覺不一致。
- 若以星星表示層級或影響深度，需搭配文字標籤，不可只用圖示表達含義。
- 圖示需加上 `aria-hidden="true"`；若圖示本身承載層級意義，外層需提供 `aria-label`。

## 每次週更檢查

更新 `ai_industry_penetration_YYYY-MM-DD.html` 時，需檢查：

- 是否保留 `.reader-controls` 控制列。
- 是否保留 Light / Dark Mode 按鈕。
- 是否保留三段字級按鈕。
- 是否有 `@media (max-width: ...)` RWD 規則。
- 是否保留適度頁面留白與主內容最大寬度。
- 新增文字是否在小字級與大字級下都可讀。
- 新增圖表或卡片是否在手機版不溢出。
- 是否保留 Google Fonts Icons / Material Symbols 載入與一致的圖示用法。
- GSAP 無法載入時，頁面、數值、圖表 fallback 與側欄是否仍可使用。
- `prefers-reduced-motion` 啟用時，是否略過進場、數字增長與側欄動畫。
